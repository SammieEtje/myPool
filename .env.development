# ==============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT CONFIGURATION
# ==============================================================================
# This file contains settings for running the F1 Betting Pool on your laptop
# without HTTPS requirements. DO NOT use these settings in production!
#
# Usage:
#   1. Copy this file to .env: cp .env.development .env
#   2. Run migrations: python manage.py migrate
#   3. Create superuser: python manage.py createsuperuser
#   4. Start server: python manage.py runserver
#   5. Access at: http://localhost:8000
#
# ==============================================================================

# Django Core Settings
# ------------------------------------------------------------------------------
# IMPORTANT: DEBUG=True disables all HTTPS/SSL requirements for local development
DEBUG=True

# Use the default insecure key for local development (NEVER use in production!)
SECRET_KEY=django-insecure-9nwm*93!-gwh3ym*1dkvsd@b^w@-aj7@93mv@z3pzl#z1lo*9g

# Allow localhost access
ALLOWED_HOSTS=localhost,127.0.0.1,testserver

# Database Configuration
# ------------------------------------------------------------------------------
# SQLite is perfect for local development (no setup required)
DATABASE_URL=sqlite:///db.sqlite3

# Email Configuration (Development Mode)
# ------------------------------------------------------------------------------
# Console backend prints emails to terminal instead of sending them
# Perfect for testing MFA and notifications locally
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Social Authentication (Optional)
# ------------------------------------------------------------------------------
# Leave these blank if you don't need Google/GitHub login in development
# To enable social auth, get credentials from:
#   Google: https://console.cloud.google.com/
#   GitHub: https://github.com/settings/developers
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# F1 API Configuration
# ------------------------------------------------------------------------------
# OpenF1 API for fetching race data and results
F1_API_BASE_URL=https://api.openf1.org/v1

# Security Settings (Automatically Disabled in DEBUG Mode)
# ------------------------------------------------------------------------------
# When DEBUG=True, the following are automatically disabled:
#   - SECURE_SSL_REDIRECT (no HTTP to HTTPS redirect)
#   - SECURE_HSTS_* (no HSTS enforcement)
#   - SESSION_COOKIE_SECURE (cookies work over HTTP)
#   - CSRF_COOKIE_SECURE (CSRF tokens work over HTTP)
#
# This allows you to run the app on http://localhost:8000 without any issues!
